$TTL <%= ttl %>
$ORIGIN <%= zone %>.
;<%= zone %>. IN SOA <%= soa %>. <%= contact %> (
@ 1D IN SOA <%= soa %>. <%= contact %> (
  <%= serial %> ;Serial
    3h       ;Refresh
    15       ;Retry
    1w       ;Expire
    3h       ;Negative caching TTL
  )


<%= zone %>. IN  NS <%= soa %>.
<%= soa %>. IN  A <%= soaip %>

